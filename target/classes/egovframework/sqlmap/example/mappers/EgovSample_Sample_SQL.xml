<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.HGH_SampleMapper">

	<resultMap id="sample" type="egovframework.example.sample.service.HGH_SampleVO">
		<result property="id" column="HGH_id"/>
		<result property="name" column="HGH_name"/>
		<result property="description" column="HGH_description"/>
		<result property="useYn" column="HGH_use_yn"/>
		<result property="regUser" column="HGH_reg_user"/>
	</resultMap>

	<insert id="insertSample" parameterType="SampleVO">

			INSERT INTO HGH_BOARD
				( HGH_ID
				  , HGH_NAME
				  , HGH_DESCRIPTION
				  , HGH_USE_YN
				  , HGH_REG_USER )
			VALUES ( #{id}
				  , #{name}
				  , #{description}
				  , #{useYn}
				  , #{regUser} )

	</insert>

	<update id="updateSample">

			UPDATE HGH_BOARD
			SET HGH_ID=#{id}
				, HGH_NAME=#{name}
				, HGH_DESCRIPTION=#{description}
				, HGH_USE_YN=#{useYn}
				  HGH_WHERE ID=#{id}

	</update>

	<delete id="deleteSample">

			DELETE FROM HGH_BOARD
			WHERE HGH_ID=#{id}

	</delete>

	<select id="selectSample" resultMap="sample">

			SELECT
				HGH_ID, HGH_NAME, HGH_DESCRIPTION, HGH_USE_YN, HGH_REG_USER
			FROM HGH_BOARD
			WHERE HGH_ID=#{id}

	</select>

	<select id="selectSampleList" parameterType="searchVO" resultType="egovMap">

			SELECT
				HGH_ID, HGH_NAME, HGH_DESCRIPTION, HGH_USE_YN, HGH_REG_USER
			FROM HGH_BOARD
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND	HGH_ID LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	HGH_NAME LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
			ORDER BY HGH_ID ASC
			LIMIT #{recordCountPerPage} OFFSET #{firstIndex}
	</select>

	<select id="selectSampleListTotCnt" parameterType="searchVO" resultType="int">

			SELECT COUNT(*) totcnt
			FROM HGH_BOARD
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND HGH_ID LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	HGH_NAME LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
	</select>

</mapper>